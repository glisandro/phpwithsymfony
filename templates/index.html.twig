{% extends 'base.html.twig' %}

{% block body_id 'blog_index' %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('build/js/app.js') }}"></script>
{% endblock %}

{% block main %}
   <nav class="navbar navbar-expand-md bg-primary navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="#">Navbar</a>
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbar2SupportedContent" aria-controls="navbar2SupportedContent" aria-expanded="false" aria-label="Toggle navigation"> <span class="navbar-toggler-icon"></span> </button>
      <div class="collapse navbar-collapse text-center justify-content-end" id="navbar2SupportedContent">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="#"><i class="fa d-inline fa-lg fa-bookmark-o"></i> Bookmarks</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#"><i class="fa d-inline fa-lg fa-envelope-o"></i> Contacts</a>
          </li>
        </ul>
        <a class="btn navbar-btn btn-primary ml-2 text-white"><i class="fa d-inline fa-lg fa-user-circle-o"></i> Sign in</a>
      </div>
    </div>
  </nav>
    {% for project in projects %}
        <article class="post">
            <h2>
                <a href="{{ project.url }}" target="_blank">

                     {{ project.title }}
                </a>
            </h2>
        </article>
    {% else %}
        <div class="well"> project.no_posts_found|trans </div>
    {% endfor %}

    0 down time deploy
    prueba

    chuletas

    <h4>Comandos linux</h4>
    <a href="http://www.linuxguide.it/command_line/linux_commands_es.html">http://www.linuxguide.it/command_line/linux_commands_es.html</a>
    

    <h4>Para revisar areas con problemas en una web</h4>
    <a href="http://webdevchecklist.com/"></a>
    
 
{% endblock %}

{% block sidebar %}


{% endblock %}
